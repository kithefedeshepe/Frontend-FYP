# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_COVID_19_IMAGING_SYSTEM }}'
          channelId: live
          projectId: covid-19-imaging-system
      - name: Clear Cache
        run: |
          echo "Clearing cache..."
          rm -rf ${{ runner.workspace }}/node_modules
          rm -rf ${{ runner.workspace }}/build
        # Add more cache-clearing commands if needed

      - name: Checkout Code
        uses: actions/checkout@v2
